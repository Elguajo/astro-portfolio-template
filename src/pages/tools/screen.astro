---
import BaseLayout from '@/layouts/BaseLayout.astro';
import ScreenComponent from '@hdud/common/pages/screen.astro';
import { siteConfig } from '@/site.config';

const mates = {
  title: 'screen',
  description:
    'A diagnostic overlay to inspect responsive breakpoints and layout dimensions.',
  author: siteConfig.author,
  email: siteConfig.email,
  local: 'en',
  ogImage: '/og-image/site-image.png',
};
---

<BaseLayout {...mates}>
  <ScreenComponent />
</BaseLayout>
